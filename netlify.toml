[build]
  command = "pnpm build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--version"

# SPA 라우팅 대응 (Vue Router 사용 시 새로고침 404 방지)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
